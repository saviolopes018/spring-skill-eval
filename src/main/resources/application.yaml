spring:
  application:
    name: spring-skill-eval

  ai:
    model:
      chat: ${SPRING_AI_MODEL_CHAT:none}

    openai:
      api-key: ${OPENAI_API_KEY:}

      chat:
        model: ${OPENAI_MODEL:gpt-5-mini}
