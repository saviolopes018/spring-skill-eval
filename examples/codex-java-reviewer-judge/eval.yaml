schema_version: v1alpha1

name: codex-java-reviewer-judge

skill:
  path: ./skill

engine:
  type: process
  command: codex
  args:
    - exec
    - "--"

cases:
  - ./cases/null-safety.yaml

defaults:
  timeout: 120s
